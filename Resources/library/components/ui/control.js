'use strict';

var Zen = require('/library/zen_ti');

var Element = Zen.import('ui.element');

function Control(configuration) {
    
}

Zen.extend(Control, Element);

module.exports = Control;